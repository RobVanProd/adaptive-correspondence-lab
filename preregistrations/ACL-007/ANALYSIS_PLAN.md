# ACL-007 analysis plan

## Frozen sequence

1. Abort before RNG use unless exact approved HEAD, fully clean porcelain, canonical
   bundle, canonical nonexistent evidence path, exact lock/directory membership, pinned
   environment, source ACL-006 hashes, and tolerant analytic registry all pass.
2. Generate every target through all fixed checkpoints
   `8192,32768,131072,262144`; no outcome-dependent stopping occurs.
3. Store per-chunk count, direction sum, outer-product sum, terminal support-mask counts,
   and terminal PCG64 state. Reconstruct every checkpoint from sufficient statistics.
4. Apply standardized-mean, dissociation, and contrast components independently, then
   apply the frozen overall transport rule.
5. Write exactly one previously nonexistent SHA-derived artifact.

## Standardized-mean component

For full and two disjoint half means, compute

\[
Z_2=\frac{\|\bar d-m\|_2}{\sqrt{\operatorname{tr}(\Sigma)/L}}.
\]

PASS requires every score at most `5`, Type-7 full-score median at most `1.5`, Type-7
Q90 at most `2.5`, and every truth-cosine residual within the exact registry envelope.

## Dissociation component

Only locked IDs `A06,A07,A08` gate. PASS requires every one to have final half cosine
at least `0.995` and observed truth cosine at most `0.95`. An empty stratum is INVALID.

## Contrast component

All nine locked contrasts are analytically resolvable. Each must reproduce the sign of
the exact right-minus-left truth-cosine gap and retain absolute gap at least `0.10`.

## Overall verdict

PASS requires all three components PASS. Any component failure is FAIL. INVALID is
reserved for violated frozen semantics such as an empty required stratum. Stress or
secondary summaries cannot rescue a failure.

The target set is a deterministic benchmark, not a random landscape sample. Type-7
summaries are descriptive gates, not population-confidence claims. No target coefficient
is fit and no posterior interval is reported as a scientific population statement.
